# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# .gitignore 파일

# 환경 변수 파일을 무시하도록 설정
.env
*.env
**/.env
**/.env.*
.env.*

# Common secrets & credentials (never commit)
# Android keystore & signing
android/key.properties
**/*.keystore
**/*.keystore.properties
**/*.jks

# Firebase service accounts (if any are downloaded)
**/*serviceAccount*.json
**/firebase-adminsdk-*.json
**/*-adminsdk-*.json

# Firebase config files (optional to ignore; if ignored, teammates must regenerate locally)
# Note: Firebase API keys are not secrets, but you can ignore these files if you prefer not to commit them.
/lib/firebase_options.dart
/android/app/google-services.json
/ios/Runner/GoogleService-Info.plist

# Flutter ephemeral build outputs (can contain plugin example configs)
**/flutter/ephemeral/